<div class="container">
  All books:
  <% @books.each do |book| %>
    <ul class="list-group">
      <li class="list-group-item"> <%= link_to(book.title, book_path(book)) %> </li>
    </ul>
  <% end %>
</div>
