<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <% if user_signed_in? %>
    <a class="navbar-brand" href="<%= home_index_path %>">Home</a>
  <% elsif staff_signed_in? %>
    <a class="navbar-brand" href="<%= staff_root_path %>">Home</a>
  <% end %>
  <div id="navbarNav">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item">
          <a class="nav-link" href="<%= books_path %>">Books</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= loans_path %>">Loans</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= branches_path %>">Libraries</a>
        </li>
        <li class="nav-item">
          <%= link_to('Logout', destroy_user_session_path, method: :delete, class: "nav-link") %>
        </li>
      <% elsif staff_signed_in? %>
        <li class="nav-item">
          <%= link_to('Logout', destroy_staff_session_path, method: :delete, class: "nav-link") %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
